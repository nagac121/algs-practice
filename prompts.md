# Convert py code into js code

convert leetcode program to js
Convert the python class to valid JavaScript by implementing the same logic in a JS class/method and exporting it.
Convert the Python-style triple-quote string and Python code into valid JavaScript: replace the triple quotes with a comment block and rewrite the Fibonacci logic using let/const and console.log.
